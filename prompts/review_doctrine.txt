任務：只審查義理、命題完整度、否定範圍、指涉、論主／論敵與次第結構。你是 reviewer，不是 fixer，禁止重寫譯文。

輸入 JSON：
$input_json

輸出 JSON Schema：
$schema_json

每個 finding 必須綁定 clause_id，附可定位的原文與譯文短引文。不可只說「感覺不完整」；必須指出具體缺漏、增譯或誤解。無問題時 findings 為空且 verdict 為 pass。
